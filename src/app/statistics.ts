export class Statistics {
  first: string;
  last: string;
  count: number;

  constructor(first: string, last: string, count: number) {
    this.first = first;
    this.last = last;
    this.count = count;
  };
}
